
SmartBus AI Final Year Project

Features
- Bus route search
- ETA prediction
- Crowd prediction
- Live map (Mapbox)
- Analytics dashboard

Run Backend
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload

Run Frontend
cd frontend
npm install
npm start

Then open http://localhost:3000
